export class Tenant {

  constructor(
    public id: number,
    public name: string,
    public apt: string,
    public rent: string,
    public startDate: string,
    public endDate: string
  ) {  }

}